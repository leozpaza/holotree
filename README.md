# ◈ HoloTree — Интерактивная База Знаний

<p align="center">
  <img src="https://img.shields.io/badge/React-18.3-61DAFB?style=flat-square&logo=react" />
  <img src="https://img.shields.io/badge/Node.js-22+-339933?style=flat-square&logo=node.js" />
  <img src="https://img.shields.io/badge/SQLite-3-003B57?style=flat-square&logo=sqlite" />
  <img src="https://img.shields.io/badge/Socket.io-4.8-010101?style=flat-square&logo=socket.io" />
</p>

## 🌟 Описание

HoloTree — это современная вики-система с уникальной визуализацией связей между статьями в виде интерактивного графа. Система создана для команд техподдержки, разработчиков и организаций, которым нужен быстрый доступ к структурированной документации.

### Ключевые особенности

- **🌳 Голографическое дерево** — визуализация базы знаний как интерактивного графа с узлами и связями
- **⚡ Real-time редактирование** — совместное редактирование статей а-ля Google Docs с отображением курсоров других пользователей
- **🔍 Мгновенный поиск** — live-поиск по заголовкам, тегам и содержимому с подсветкой результатов на графе
- **🏷️ Система тегов** — гибкая категоризация статей с автодополнением
- **📁 Загрузка файлов** — drag & drop изображений и файлов прямо в редактор
- **🌙 Голографический дизайн** — тёмная тема с неоновыми акцентами и плавными анимациями

## 📋 Технический стек

| Компонент | Технология |
|-----------|------------|
| Frontend | React 18 + Vite |
| Визуализация графа | React Flow (@xyflow/react) |
| Анимации | Framer Motion |
| Редактор | Tiptap (ProseMirror) |
| Real-time синхронизация | Yjs + Socket.io |
| Backend | Node.js + Express |
| База данных | SQLite (sql.js) |
| Загрузка файлов | Multer |

## 🚀 Быстрый старт

### Требования

**Вариант 1: Docker (Рекомендуется)**
- Docker и Docker Compose

**Вариант 2: Локальная установка**
- Node.js 18+ 
- npm или yarn

### Установка и запуск

#### 🐳 Запуск с Docker (Рекомендуется)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd holotree

# Запустите через Docker Compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

**Преимущества Docker:**
- ✅ Автоматическое сохранение БД через volumes
- ✅ Изолированная среда
- ✅ Простое развёртывание на production

#### 💻 Локальная установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd holotree

# Запустите одной командой
chmod +x start.sh
./start.sh

# Или вручную:

# 1. Установка зависимостей бэкенда
cd backend
npm install

# 2. Установка и сборка фронтенда
cd ../frontend
npm install
npm run build

# 3. Копирование билда в бэкенд
cp -r dist/* ../backend/public/

# 4. Запуск сервера
cd ../backend
node server.js
```

Откройте в браузере: **http://localhost:3001**

## 📖 Использование

### Навигация по дереву

- **Зум**: Колесико мыши или pinch на тачпаде
- **Перемещение**: Зажмите ЛКМ и перетаскивайте холст
- **Мини-карта**: В правом нижнем углу для быстрой навигации

### Управление узлами

- **Открыть статью**: Клик на узел
- **Создать дочерний узел**: Наведите на узел → кнопка "+"
- **Удалить узел**: Наведите на узел → кнопка "×"

### Редактирование

- **Форматирование**: Панель инструментов над редактором (H1, H2, **Bold**, *Italic*, `code`)
- **Изображения**: Перетащите изображение в область редактора
- **Теги**: Введите тег и нажмите Enter

### Поиск

Введите запрос в поле поиска в шапке. Результаты появятся в выпадающем списке, а камера плавно переместится к найденному узлу.

## 📁 Структура проекта

```
holotree/
├── backend/
│   ├── server.js          # Express + Socket.io сервер
│   ├── holotree.db        # SQLite база данных
│   ├── uploads/           # Загруженные файлы
│   │   ├── images/
│   │   └── files/
│   └── public/            # Собранный фронтенд
├── frontend/
│   ├── src/
│   │   ├── App.jsx        # Главный компонент
│   │   ├── components/
│   │   │   ├── KnowledgeTree.jsx   # Граф дерева
│   │   │   ├── HoloNode.jsx        # Узел графа
│   │   │   ├── ArticleModal.jsx    # Редактор статьи
│   │   │   ├── SearchBar.jsx       # Поиск
│   │   │   └── UserPresence.jsx    # Аватары пользователей
│   │   └── styles/
│   └── vite.config.js
└── start.sh               # Скрипт запуска
```

## 🗄️ Структура базы данных

### Таблица `nodes`
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT (UUID) | Первичный ключ |
| parent_id | TEXT | Ссылка на родительский узел |
| title | TEXT | Заголовок статьи |
| content | TEXT (JSON) | Содержимое в формате Tiptap/Yjs |
| position_x, position_y | REAL | Позиция на графе |
| created_at, updated_at | TEXT | Временные метки |
| author_id, last_editor_id | TEXT | ID авторов |

### Таблица `tags`
| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Первичный ключ |
| name | TEXT | Уникальное имя тега |

### Таблица `node_tags` (M2M связь)
| Поле | Тип |
|------|-----|
| node_id | TEXT |
| tag_id | INTEGER |

### Таблица `assets`
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT (UUID) | Первичный ключ |
| file_path | TEXT | Путь к файлу |
| original_name | TEXT | Оригинальное имя |
| mime_type | TEXT | MIME-тип |
| size | INTEGER | Размер в байтах |
| node_id | TEXT | Привязка к статье |

## 🔌 API Endpoints

### REST API

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/nodes` | Получить все узлы |
| GET | `/api/nodes/:id` | Получить узел по ID |
| POST | `/api/nodes` | Создать узел |
| PUT | `/api/nodes/:id` | Обновить узел |
| DELETE | `/api/nodes/:id` | Удалить узел |
| GET | `/api/search?q=` | Поиск по узлам |
| GET | `/api/tags` | Получить все теги |
| POST | `/api/upload` | Загрузить файл |

### WebSocket Events

| Событие | Направление | Описание |
|---------|-------------|----------|
| `user:init` | Server → Client | Инициализация пользователя |
| `users:update` | Server → All | Обновление списка пользователей |
| `node:created` | Server → All | Новый узел создан |
| `node:updated` | Server → All | Узел обновлён |
| `node:deleted` | Server → All | Узел удалён |
| `node:join` | Client → Server | Присоединиться к редактированию |
| `node:sync` | Server → Client | Синхронизация Yjs документа |
| `cursor:update` | Bidirectional | Позиция курсора |

## 💾 Система бэкапов

HoloTree включает **многоуровневую систему защиты данных**:

### Автоматические механизмы

1. **Автосохранение БД**: каждые 10 секунд
2. **Автоматические бэкапы**: каждые 5 минут (настраивается)
3. **Ротация бэкапов**: хранятся последние 50 бэкапов (настраивается)
4. **Graceful shutdown**: корректное сохранение при остановке сервера
5. **Docker volumes**: постоянное хранение данных при использовании Docker

### Управление бэкапами

#### Через CLI утилиту

```bash
# Список всех бэкапов
node backend/backup-utils.js list

# Создать бэкап вручную
node backend/backup-utils.js create

# Восстановить из бэкапа
node backend/backup-utils.js restore holotree-2025-12-23T15-30-00.db

# Очистить старые бэкапы (оставить 30)
node backend/backup-utils.js clean 30

# Экспорт в JSON
node backend/backup-utils.js export
```

#### Через Shell скрипты

```bash
# Полный бэкап (БД + загруженные файлы)
./scripts/backup.sh

# Восстановление из полного бэкапа
./scripts/restore.sh full-backup-2025-12-23_15-30-00.tar.gz
```

#### Через REST API

```bash
# Список бэкапов
GET /api/backups

# Создать бэкап
POST /api/backup/create

# Восстановить
POST /api/backup/restore
Body: { "backupPath": "/path/to/backup.db" }
```

### Где хранятся данные

```
backend/
├── data/
│   └── holotree.db          # Основная база данных
├── backups/
│   ├── holotree-*.db        # Автоматические бэкапы БД
│   └── full-backup-*.tar.gz # Полные бэкапы (БД + файлы)
└── uploads/                  # Загруженные файлы
    ├── images/
    ├── files/
    └── thumbs/
```

### Интеграция с облаком

Для синхронизации с **Google Drive**, **Dropbox** или другим облаком:

```bash
# Установите rclone
curl https://rclone.org/install.sh | sudo bash

# Настройте remote
rclone config

# Автоматическая синхронизация бэкапов
# Добавьте в crontab:
*/30 * * * * rclone sync /path/to/backend/backups remote:holotree-backups
```

## ⚙️ Конфигурация

Переменные окружения:

```bash
# Сервер
PORT=3001                      # Порт сервера
NODE_ENV=production            # Окружение

# База данных
DB_PATH=/app/data/holotree.db  # Путь к БД
BACKUP_DIR=/app/backups        # Директория бэкапов
UPLOADS_DIR=/app/uploads       # Директория загрузок

# Бэкапы
BACKUP_INTERVAL=300000         # Интервал автобэкапа (мс), 5 минут
BACKUP_KEEP_COUNT=50           # Количество хранимых бэкапов
```

### Docker volumes

При использовании Docker данные сохраняются в:

```yaml
volumes:
  holotree-data:      # База данных
  holotree-uploads:   # Загруженные файлы  
  holotree-backups:   # Бэкапы
```

Монтируются в хост-систему в `backend/data`, `backend/uploads`, `backend/backups`

## 🎨 Кастомизация темы

Цветовая схема определена в `/frontend/src/styles/global.css`:

```css
:root {
  --bg-deep: #0a0e1a;           /* Глубокий фон */
  --bg-primary: #0f172a;        /* Основной фон */
  --cyan-glow: #00ffff;         /* Циановый акцент */
  --magenta-glow: #ff00ff;      /* Пурпурный акцент */
  --neon-green: #00ff88;        /* Зелёный акцент */
}
```

## 📝 Лицензия

MIT License

---

<p align="center">
  Создано с ◈ для команд, которые ценят структурированные знания
</p>
